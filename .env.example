# Optional — only needed for a live Connect sandbox call.
# Copy to .env or set in the shell. Do not commit real credentials.

CONNECT_BASE_URL=https://connect.sandbox.creditsafe.com
USERNAME=
PASSWORD=
# CONNECT_USERNAME=
# CONNECT_PASSWORD=
